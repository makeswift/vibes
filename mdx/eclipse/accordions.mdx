---
title: Accordions
preview: eclipse/accordions
---

## Installation

<Steps>
    <Step>
        ### Install dependencies

        ```bash
        npm install @radix-ui/react-accordion
        ```

    </Step>
    <Step>
        ### Copy and paste component code

        ```tsx
        This is the code
        ```

    </Step>
    <Step>
        ### Update tailwind.config.js

        ```tsx
        /** @type {import('tailwindcss').Config} */
        module.exports = {
        theme: {
            extend: {
            keyframes: {
                "accordion-down": {
                from: { height: "0" },
                to: { height: "var(--radix-accordion-content-height)" },
                },
                "accordion-up": {
                from: { height: "var(--radix-accordion-content-height)" },
                to: { height: "0" },
                },
            },
            animation: {
                "accordion-down": "accordion-down 0.2s ease-out",
                "accordion-up": "accordion-up 0.2s ease-out",
            },
            },
        },
        }
        ```
    </Step>

</Steps>

## Usage

```tsx
This is the code
```

## API Reference

| Prop      | Type     | Default    |
| --------- | -------- | ---------- |
| `type`    | `enum`   | `multiple` |
| `testing` | `string` | —          |
| `testing` | `string` | —          |
| `testing` | `string` | —          |

## Makeswift integration

```tsx
import dynamic from 'next/dynamic'

import { List, RichText, Select, Shape, Slot, Style } from '@makeswift/runtime/controls'
import { forwardNextDynamicRef } from '@makeswift/runtime/next'

import { runtime } from '@/lib/makeswift/runtime'

runtime.registerComponent(
  forwardNextDynamicRef(patch =>
    dynamic(() => patch(import('./Accordions').then(({ Accordions }) => Accordions)))
  ),
  {
    type: 'accordions',
    label: 'Folder 2 / Accordions',
    props: {
      className: Style(),
      accordions: List({
        label: 'Accordions',
        type: Shape({
          type: {
            title: RichText({
              mode: RichText.Mode.Inline,
            }),
            body: Slot(),
          },
        }),
        getItemLabel() {
          return 'Slot'
        },
      }),
      type: Select({
        label: 'Type',
        options: [
          { label: 'Single', value: 'single' },
          { label: 'Multiple', value: 'multiple' },
        ],
        defaultValue: 'multiple',
      }),
    },
  }
)
```

## Figma

<div className="pattern-shadow pattern-shadow-sm mb-5 mt-5 aspect-[4/3] border border-docs-foreground bg-docs-background">
  <iframe
    src="https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2FSs4o7sfbgqVr83CTXG7vRa%2FEclipse%3Ftype%3Ddesign%26node-id%3D23%253A36%26mode%3Ddesign%26t%3D3OZNQn2PpZT6AfPq-1"
    className="h-full w-full bg-docs-background"
  ></iframe>
</div>

{/* https://www.figma.com/file/Ss4o7sfbgqVr83CTXG7vRa/Eclipse?type=design&node-id=23%3A36&mode=design&t=3OZNQn2PpZT6AfPq-1 */}
